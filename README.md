# POO
programación orientada a objetos
crear una clase que el objeto sea puerta con atributos (color, tamaño )con un metodo donde  se abra en un dia y hora especifica y un segundo metodo que indique cuando este cerrada muestre que este cerrada y  coloque hora y horario de cuando este abierta